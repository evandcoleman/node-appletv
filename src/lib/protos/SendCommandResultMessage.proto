syntax = "proto2";

import "ProtocolMessage.proto";

extend ProtocolMessage {
  optional SendCommandResultMessage commandResultMessage = 7;
}

message SendCommandResultMessage {
  optional uint64 value = 1;
}